fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
python-dotenv==1.0.1
python-multipart==0.0.20
requests==2.32.3
streamlit==1.40.2
sarvamai==0.1.30
pytest==8.3.4
pytest-mock==3.14.0
httpx==0.28.1
bcrypt==4.0.1
hypothesis==6.151.9

# Retry/backoff around real outbound Sarvam AI calls (see backend/services/sarvam_client.py) --
# already an installed transitive dependency (of langgraph/langchain-core below); pinned
# explicitly here now that sarvam_client.py imports it directly for its own retry decorator.
tenacity==9.1.2
reportlab==5.0.0  # worker complaint-resolution report PDF generation, see backend/services/complaint_report_service.py

# Error monitoring (see backend/main.py's init_error_monitoring()) -- OFF by default, only
# activates when SENTRY_DSN is actually set, matching this codebase's existing "off unless
# explicitly configured" pattern for optional external services (LangSmith tracing below, Sarvam
# itself). sentry-sdk's fastapi extra pulls in nothing beyond sentry-sdk itself -- it's just the
# integration module, no new transitive dependency tree. Bumped from the first-pinned 2.19.2 to
# the latest 2.68.0 to pick up Logs/Metrics/Profiling (enable_logs, enable_metrics,
# profile_session_sample_rate) -- confirmed directly against 2.68.0's own ClientConstructor that
# all three are real, current options, not deprecated/renamed ones copied from stale docs.
sentry-sdk[fastapi]==2.68.0

# RAG retrieval (Ask Sarthi) -- see docs/ask_janmitra_rag_architecture.md.
# torch is CPU-only (~200MB) -- installing plain `pip install torch` on Windows pulls the much
# larger CUDA wheel by default; install it explicitly first with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# before `pip install -r requirements.txt`, or the sentence-transformers install below will pull
# in the default (CUDA) torch build instead.
torch==2.13.0
sentence-transformers==5.7.0
chromadb==1.5.9
numpy==2.3.0
# Hybrid search: BM25 keyword scoring alongside the existing vector search (see
# rag_retriever.py's own module docstring for why) -- pure Python, no server, no GPU, indexes the
# already-filtered candidate pool in-process per query. Already installed with no dependents of
# its own (confirmed via `pip show`), now pinned explicitly since RagRetriever imports it directly.
rank-bm25==0.2.2

# Ask Sarthi orchestration layer -- see docs/ask_janmitra_orchestration.md and
# backend/services/orchestration/. LangGraph is the workflow-orchestration engine (StateGraph,
# conditional routing); langchain-core is a direct dependency of langgraph itself (RunnableConfig)
# and is not otherwise used as a service layer -- see that doc for the "why not LangChain as the
# orchestrator" reasoning.
langgraph==1.2.10
langchain-core==1.5.3

# LangSmith observability layer around the Ask Sarthi LangGraph pipeline -- see
# backend/services/observability/tracing.py and docs/ask_janmitra_langsmith_observability.md.
# Already an installed transitive dependency of langchain-core; pinned explicitly here now that
# this app imports it directly (backend/services/observability/tracing.py).
langsmith==0.10.17

# Arize Phoenix -- second, self-hosted tracing backend alongside LangSmith (see
# backend/services/observability/tracing.py's module docstring for why: LangSmith's free monthly
# trace quota running out shouldn't stop observability entirely). Both packages below are
# lightweight (openinference-semantic-conventions + opentelemetry-sdk, confirmed directly) --
# NEITHER is the full `arize-phoenix` server package, which pulls a much heavier dependency tree
# (its own FastAPI/SQLAlchemy/etc.) that conflicts with this project's own pinned versions. Run
# the actual Phoenix server separately (its own isolated venv or Docker container), never inside
# this project's environment -- see PHOENIX_TRACING_PLAN.md.
arize-phoenix-otel==0.17.1
# REST client (Prompts/Datasets/Experiments -- see scripts/push_prompts_to_phoenix.py and
# scripts/phoenix_rag_evaluation.py). Confirmed separately lightweight from arize-phoenix-otel
# above -- installing it added zero new heavy dependencies (everything it needs was already
# pulled in by arize-phoenix-otel).
arize-phoenix-client==3.3.0

# Auto-instruments LangGraph/LangChain itself (every internal graph node -- input_processing,
# language_detection, intent_classification, etc., plus the _route_after_* conditional edges) so
# Phoenix gets the SAME fine-grained, framework-level trace LangSmith already captures
# automatically via its own LangChain integration -- previously Phoenix only ever saw this app's
# own hand-built spans (ask_janmitra_graph/rag_retrieval/answer_generation/...), a real gap
# LIVE-REPORTED by comparing an actual LangSmith trace screenshot against Phoenix's spans for the
# same request. Confirmed lightweight via `pip install --dry-run` before installing: every one of
# its own dependencies was already satisfied by arize-phoenix-otel above -- adds exactly one new
# package, nothing else.
openinference-instrumentation-langchain==0.1.72

# Real speech-to-text cost tracking -- see backend/services/audio_duration.py. Pure-Python, no
# system binaries (unlike pydub/ffmpeg); confirmed it has NO WebM support at all, which
# audio_duration.py's own hand-rolled EBML fallback covers.
mutagen==1.48.1

# Ask Sarthi image understanding -- see backend/services/vision_service.py. Sarvam (this
# project's only other AI vendor) has no vision/image-understanding capability (checked directly
# against the installed SDK's type stubs); the user chose a local open-source vision-language
# model over a new hosted vendor/API key, reusing the torch dependency already installed above.
# transformers is the model-loading library (same role sentence-transformers plays for text
# embeddings); pillow decodes uploaded image bytes; accelerate is a transformers dependency for
# device placement (CPU here -- see vision_service.py's module docstring for why no GPU/
# quantization is required on this project's deployment target).
transformers==4.57.1
pillow==11.0.0
accelerate==1.2.1

# MCP (Model Context Protocol) tool wrapper around the RAG/complaint services (see
# backend/mcp_server.py's module docstring) -- the official SDK, run standalone via
# `python -m backend.mcp_server`, never imported by the live FastAPI app itself. Already
# installed with no dependents of its own (confirmed via `pip show`), now pinned explicitly.
mcp==1.29.1
